🔹 DOMAIN: auth

Responsabilità

identity

sessione

login / logout

AuthIdentity = {
  subjectType: "visitor" | "user"
  subjectId
}

❌ auth non:

scrive legal

crea visitor

conosce configuration