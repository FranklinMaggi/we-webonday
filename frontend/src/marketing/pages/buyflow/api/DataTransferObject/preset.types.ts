// ======================================================
// FE || PRESET || PUBLIC TYPES
// ======================================================

export type SitePresetPublicDTO = {
    id: string;
    solutionId: string;
  
    name: string;
    description: string;
  
    preview: {
      heroImageKey: string;
    };
  };
  