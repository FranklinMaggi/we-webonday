// ======================================================
// FE || PRESET || CLASS REGISTRY
// ======================================================

export const presetClasses = {
    selector: "preset-selector",
  
    header: "preset-selector__header",
    title: "preset-selector__title",
    subtitle: "preset-selector__subtitle",
  
    grid: "preset-grid",
  
    card: "preset-card",
    preview: "preset-card__preview",
    content: "preset-card__content",
    titleText: "preset-card__title",
    description: "preset-card__description",
  
    actions: "preset-card__actions",
    selectBtn: "preset-card__select",
  };
  